╔══════════════════════════════════════╗
  CI Test Summary — ${project}
╚══════════════════════════════════════╝
  Branch   : ${branch}
  Commit   : ${commitSha}
  Runner   : ${runnerOs} / ${runnerArch}

  Total    : ${totalTests}
  Passed   : ${passedTests}
  Failed   : ${failedTests}
  Skipped  : ${skippedTests}
  Duration : ${durationSecs}s
  Coverage : ${coveragePercent}%

  Status: ${overallStatus}