INVOICE
═══════════════════════════════════
Invoice No : ${invoiceNumber}
Date       : ${invoiceDate}
Due Date   : ${dueDate}

Bill To:
  ${billingName}
  ${billingAddress}
  ${billingCity}, ${billingCountry}

  Item: ${itemDescription}
  Qty : ${itemQty} x $${itemUnitPrice}

  Subtotal : $${subtotal}
  Tax (${taxRate}%) : $${taxAmount}
  ─────────────────────
  TOTAL    : $${totalDue}

Payment method: ${paymentMethod}
Reference     : ${paymentRef}
