[${severity}] SECURITY ALERT — ${alertType}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Alert ID        : ${alertId}
Detected At     : ${detectedAt} UTC
Affected Asset  : ${affectedAsset}
Source IP       : ${sourceIp}
Actor           : ${actorIdentity}
Action          : ${action}
Resource        : ${resource}
Risk Score      : ${riskScore}/100

Description:
  ${description}

Recommended Action:
  ${recommendedAction}

Assigned To : ${assignee}
Ticket      : ${ticketUrl}
